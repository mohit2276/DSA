class Solution {
    public int mostWordsFound(String[] sentences) {
        int size=sentences.length;
        int maxxx=0;
        for(int i=0;i<size;i++){
            String[] array=sentences[i].split(" ");
            maxxx=Math.max(maxxx,array.length);
                
        }
        return maxxx;
        
    }
}
