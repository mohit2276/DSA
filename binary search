class Solution {
    public int search(int[] nums, int target) {
        int sum=-1;
        for(int i=0;i<nums.length;i++){
            if(nums[i]==target){
                sum=sum+i+1;
            }
           
            
        }
        
        return sum;
        
        
    }
}
